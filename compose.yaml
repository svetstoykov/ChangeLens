name: changelens-development

# ChangeLens currently has no required containerized services. SQLite is embedded,
# and Git, repository SDKs, and the desktop toolchain must run on the host.
services: {}
